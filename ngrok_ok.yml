version: "2"
authtoken: 37ceTBK9EoAiHk7e4Fux3V9HkVD_sHf6QH8YaVZgYzSLq4HB

tunnels:
  # --- BACKEND (Puerto 8001) ---
  # Este usa tu dominio fijo. NO lo cambies.
  server-api:
    proto: http
    addr: 8001
    hostname: karlee-insertional-pseudoprincely.ngrok-free.dev

  # --- FRONTEND (Puerto 3000) ---
  # Este NO lleva hostname. Ngrok creará una URL nueva cada vez.
  # (Así evitamos el conflicto de "dirección duplicada")
  client-app:
    proto: http
    addr: 3000